export interface Nav_itype {
  name: string;
  url: string;
}

export interface Breadcrumb_Itype {
  name: string;
  slug?: string;
}

export interface FancyItem_itype {
  name: string;
  ex: string;
  value: string;
}
